namespace TechnicalInterview.Exercice2
{
    public class ClosestToZeroDeterminator
    {
        private readonly int[] _inputs;

        public ClosestToZeroDeterminator(int[] inputs)
        {
            _inputs = inputs;
        }

        /// <summary>
        /// Return the number closest to zero from the given inputs.
        /// 
        /// Positive number has priority over negative if absolute values are equal.
        /// </summary>
        /// <returns>The number closest to zero from the given inputs</returns>
        public int Determine()
        {
            return 0;
        }
    }
}
